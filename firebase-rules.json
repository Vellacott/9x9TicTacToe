{
  "rules": {
    "games": {
      "$gameId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['player1', 'currentPlayer', 'localBoards', 'wonBoards'])"
      }
    }
  }
}

